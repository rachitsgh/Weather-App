var Domain = require("crs").Domain;

var domain = new Domain(builder);

domain.on / once("AggreType","id001","change name",function(name){
	 
});

domain.exec(["command name",data],function(err,msg){

});

