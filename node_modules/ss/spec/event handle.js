module.exports = function (arg01...){

			// aggre operate
			var AggreType = this.$getAggre(typeName);
			var newAggObj = new AggreType({name:"leo"});
			AggreType.get("id002",function(obj){
				
			});
			
			// service operate
			var service = this.$getService("serviceName");
			service(args);
			
}
